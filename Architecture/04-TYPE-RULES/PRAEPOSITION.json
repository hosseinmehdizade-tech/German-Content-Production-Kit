{
  "type_rule_schema_version": "2.0.0",
  "rule_id": "praeposition@2.0.0",
  "applies_to": ["praeposition"],
  "core_fields": {
    "governs_cases": {
      "type": "array",
      "required": true,
      "nullable": false,
      "items": {"type": "string", "required": false, "nullable": false, "enum": ["Akkusativ", "Dativ", "Genitiv"]},
      "min_items": 1,
      "unique_items": true
    },
    "position": {"type": "string", "required": false, "nullable": false, "enum": ["preposed", "postposed", "circumposed", "variable"]}
  },
  "detail_fields": {
    "case_meanings": {
      "type": "array",
      "required": false,
      "nullable": false,
      "items": {
        "type": "object",
        "required": false,
        "nullable": false,
        "properties": {
          "case": {"type": "string", "required": true, "nullable": false, "enum": ["Akkusativ", "Dativ", "Genitiv"]},
          "meaning": {"type": "string", "required": true, "nullable": false, "min_length": 1}
        },
        "required_properties": ["case", "meaning"],
        "additional_properties": false
      },
      "min_items": 1
    },
    "register_notes": {"type": "array", "required": false, "nullable": false, "items": {"type": "string", "required": false, "nullable": false, "min_length": 1}, "min_items": 1}
  },
  "allow_undeclared_core_fields": false,
  "allow_undeclared_detail_fields": false,
  "conditional_requirements": [],
  "linguistic_checks": ["Governed cases are correct for the represented meanings.", "Colloquial case variation is kept in details rather than replacing the standard core."]
}
