name: NVV Batch0003 legacy materializer (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain legacy workflow status
        shell: bash
        run: |
          echo 'Legacy automatic Batch0003 materialization is intentionally disabled.'
          echo 'Generated checkpoint ZIPs are now handled as portable verified artifacts outside Git; Git stores compact checkpoint/hash metadata at milestone sync boundaries.'
          exit 1
